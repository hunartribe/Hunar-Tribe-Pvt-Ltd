# Cloudflare Pages SPA fallback.
# React Router owns /news, /news/:slug, /gallery and /p/:slug on the client.
# Without this, opening or refreshing one of those URLs returns a Pages 404.
/*    /index.html   200
